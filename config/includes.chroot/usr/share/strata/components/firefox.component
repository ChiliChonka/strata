# Firefox ESR — Debian's supported Firefox.
SUMMARY="Firefox ESR, the Firefox Debian supports"
CATEGORY="browser"
ORIGIN="debian"
PACKAGES="firefox-esr"
# Used by `browser --set` to pick the right desktop entry.
DESKTOP="firefox-esr.desktop"
NOTE="Debian Testing carries firefox-esr, not the rapid-release firefox."
